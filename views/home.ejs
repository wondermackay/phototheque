<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Accueil</title>
  <!-- Inclusion de Bootstrap via CDN -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
  <div class="container mt-5">
    <h1 class="mb-4">Bienvenue sur notre blog</h1>
    <div class="row">
      <% if(posts && posts.length > 0) { %>
        <% posts.forEach(function(post) { %>
          <div class="col-md-4 mb-4">
            <div class="card">
              <img src="https://tse4.mm.bing.net/th?id=OIP.rXt3pGKnVmO0ut_e5cTgigAAAA&pid=Api&P=0&h=180" class="card-img-top" alt="<%= post.title %>">
              <div class="card-body">
                <h3 class="card-title"><%= post.title %></h3>
                <p class="card-text"><%= post.content %></p>
                <a href="/posts/<%= post._id %>" class="btn btn-primary">Lire plus</a>
                <form action="/posts/delete/<%= post._id %>" method="POST" style="display:inline"> 
                  <buton type="submit">Supprimer</buton>
                </form>
              </div>
            </div>
          </div>
        <% }); %>
      <% } else { %>
        <p>Aucun post à afficher.</p>
      <% } %>
    </div>
  </div>
  <!-- Inclusion de JavaScript de Bootstrap via CDN -->
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>